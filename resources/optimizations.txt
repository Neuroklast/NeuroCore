# NeuroKore expression optimization rules
# format: pattern_no_spaces|replacement|LocaleMessageKey
# Applied after built-in smart rewrites. Patterns are space-stripped.
#
# Sound-neutral only: Optimize must never change the timbre.
# Hard stays hard (clamp/hardclip), soft stays soft (softclip/tube/diode).
# Do NOT add rules that swap a hard clipper for a soft one, or that insert
# filters/stages. Those are voicing decisions, not optimizations.
#
# NOTE: do not use saturate() — not a DSL function; use softclip/hardclip/tube.

x*1|x|OptIdentity
1*x|x|OptIdentity
x+0|x|OptIdentity
0+x|x|OptIdentity
x-0|x|OptIdentity
x/1|x|OptIdentity
