# NeuroCore expression optimization rules
# format: pattern_no_spaces|replacement|LocaleMessageKey
# Applied after built-in smart rewrites. Patterns are space-stripped.
# NOTE: do not use saturate() — not a DSL function; use softclip/hardclip/tube.

tanh(clamp(x,-1,1))|softclip(x)|OptSoftclipTanh
clamp(tanh(x),-1,1)|softclip(x)|OptSoftclipTanh
tanh(clamp(x,-1.0,1.0))|softclip(x)|OptSoftclipTanh
clamp(x,-1,1)|hardclip(x,1)|OptHardclipClamp
clamp(x,-1.0,1.0)|hardclip(x,1)|OptHardclipClamp
x/(1+abs(x))|softclip(x)|OptSoftclipClassic
x/sqrt(1+x*x)|softclip(x)|OptSoftclipClassic
min(1,max(-1,x))|hardclip(x,1)|OptHardclipClamp
max(-1,min(1,x))|hardclip(x,1)|OptHardclipClamp
